RewriteEngine On
RewriteBase /

# Provide index.html for folders (essential for trailingSlash: true)
DirectoryIndex index.html

# Se o link for /login, ele procura /login/index.html
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /index.html [L]
